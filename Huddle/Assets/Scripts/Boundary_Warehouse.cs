using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Boundary_Warehouse : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	void OnTriggerEnter2D(Collider2D box) {
		//if (box.gameObject.layer == 13 || box.gameObject.layer == 18) {
		//	Debug.Log ("box hit boundary resetting position");
		//	box.transform.position.Set (0, 0, 0);

		}

	}

