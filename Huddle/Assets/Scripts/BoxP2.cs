using UnityEngine;
using System.Collections;

public class BoxP2 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Physics2D.IgnoreLayerCollision (14, 15);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
