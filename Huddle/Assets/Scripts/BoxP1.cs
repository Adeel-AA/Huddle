using UnityEngine;
using System.Collections;

public class BoxP1 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Physics2D.IgnoreLayerCollision (13, 16);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
